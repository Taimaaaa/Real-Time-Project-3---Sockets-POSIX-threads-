SERVER_PORT=8080                          # port the server listens on
LATEST_VERSION=5                          # current latest software version
UPDATE_FILE_PATH=update_files/update_v5.pkg  # file to send to outdated clients
LOG_FILE_PATH=logs/server.log             # where all events are logged
MAX_CLIENTS=10                            # max simultaneous client connections
BUFFER_SIZE=1024                          # buffer size in bytes for socket communication